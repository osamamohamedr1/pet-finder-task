// Placeholder: pet_card.dart
// Reusable UI component to display pet summary (image, name, breed, favorite toggle).

/*
When implementing:
- Build PetCard widget with an onFavorite callback
- Accept a Pet model (from domain) or lightweight map
*/
